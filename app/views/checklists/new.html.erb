<div class="jumbotron bg-secondary text-white">
	<h3 class="display-5 text-center">New Checklist for <%= @vehicle.car_id %> for Event: <%= link_to @event.id, event_path(@event.id), class: "badge badge-danger ml-1"%> </h3>
	<%= render 'checklist_form', checklist: @checklist%>
</div>
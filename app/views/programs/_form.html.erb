<%= simple_form_for(@program) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :interval %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

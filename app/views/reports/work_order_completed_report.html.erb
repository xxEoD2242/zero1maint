<%= render 'report_title_box', title: "All Completed Work Orders as of #{Date.current.strftime('%v')}" %>
<%= render 'report_page_links', url: work_order_completed_report_reports_path(params.merge(format: 'pdf').permit!) %>
<%= render 'work_order_search_results', work_orders: @work_orders, title: 'All Completed Work Orders' %>
<%= render 'work_order_search_box', q: @q, action: :work_order_completed_report %>

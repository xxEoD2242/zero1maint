<div class='container border border-white rounded p-3'>
<div class="row">
  <div class="col-md-3">
    
  </div>
  <div class='col-md-6'>
    <h3>
      <span class='badge badge-warning '>
        Vehicle is Deadlined!
      </span>
    </h3>
  </div>
  <div class="col-md-3">
  </div>
</div>
  
<div class="row">
  <div class="col-md-3">
      Work Order #:
  </div>
  <div class='col-md-2'>
    <%= link_to vehicle.requests.where("status != 'Completed'").where(deadline: true).last.id, request_path(vehicle.requests.where("status != 'Completed'").where(deadline: true).last.id), class: 'badge badge-danger' %>
  </div>
  <div class="col-md-7">
    <p>
      <%= vehicle.requests.where("status != 'Completed'").where(deadline: true).last.description %>
    </p>
  </div>
</div>
  </div>
